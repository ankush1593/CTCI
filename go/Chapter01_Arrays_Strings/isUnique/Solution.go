package isunique

func main() {
	//Approach 1 : Store elements in a map and check if the element exists
	//println(isUnique("TesT"))

	//Approach 2 : Sort and check the next adjacent element is same
	//println(isUniqueWithoutExtraSpace("TestaT"))
}
